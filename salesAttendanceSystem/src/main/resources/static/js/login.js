$(document).ready(function(){
	
	$('#refrm').hide();
	
	/*$(document.body).on('click','.login',function()
		     {	
				console.log("in dso form   *************");
		        		var frmid=$(this).attr('name');
		        		console.log(frmid + "*************");
		     			$.post("DataEntryFormUtilController?action=getform&formname="+frmid+"&mstCount="+mstCount+"&levelfilter="+l+"&level="+level, function(data, status){
		     				$('#generatedForm').html('');
		     				$('#generatedForm').html(data);
		     				$('.selectpicker').selectpicker('show');
		     				console.log("in dso form   *************");
		     		});
	});*/
});